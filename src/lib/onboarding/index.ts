/**
 * Onboarding Facade - Punto de Entrada Único
 * @description Re-exporta API pública de la feature de onboarding
 * 
 * ARQUITECTURA (Patrón Híbrido - PARTE 4 de ARQUITECTURA_FOLDER.md):
 * 
 * ┌─────────────────────────────────────────────────────────────┐
 * │ routes/(app)/onboarding/                                    │
 * │ ↓ importa SOLO desde lib/onboarding (este facade)          │
 * └─────────────────────────────────────────────────────────────┘
 * 
 * ┌─────────────────────────────────────────────────────────────┐
 * │ lib/onboarding/index.ts (FACADE PÚBLICO)                    │
 * │ ↓ re-exporta desde features/ y lib/                         │
 * └─────────────────────────────────────────────────────────────┘
 * 
 * ┌─────────────────────────────────────────────────────────────┐
 * │ features/onboarding/ (implementación privada)               │
 * │ - constants/ → SECTOR_OPTIONS, TRANSFER_POLICY_DEFAULTS     │
 * └─────────────────────────────────────────────────────────────┘
 * 
 * ┌─────────────────────────────────────────────────────────────┐
 * │ lib/services/onboarding.service.ts (lógica de negocio)      │
 * │ lib/schemas/onboarding.schemas.ts (validación)              │
 * └─────────────────────────────────────────────────────────────┘
 * 
 * REGLA DE ORO:
 * ✅ routes/ → lib/onboarding (facade)
 * ❌ routes/ → lib/services/onboarding.service (violación)
 * ❌ routes/ → features/onboarding (violación)
 */

// ═══════════════════════════════════════════════════════════════════
// 📊 SCHEMAS DE VALIDACIÓN (desde lib/schemas)
// ═══════════════════════════════════════════════════════════════════

export {
  OnboardingStep1Schema,
  OnboardingStep2Schema,
  OnboardingStep3Schema,
  OnboardingCompleteSchema,
  type OnboardingStep1Input,
  type OnboardingStep2Input,
  type OnboardingStep3Input,
  type OnboardingCompleteInput,
} from '~/lib/schemas/onboarding.schemas';

// ═══════════════════════════════════════════════════════════════════
// 🏢 SERVICIO DE NEGOCIO (desde lib/services)
// ═══════════════════════════════════════════════════════════════════

export { OnboardingService } from '~/lib/services/onboarding.service';

// ═══════════════════════════════════════════════════════════════════
// 📚 CONSTANTES Y OPCIONES (desde features/onboarding)
// ═══════════════════════════════════════════════════════════════════

export {
  SECTOR_OPTIONS,
  TRANSFER_POLICY_DEFAULTS,
  PHONE_OPTIONS,
  type PhoneOption,
} from '~/features/onboarding/constants';

// ═══════════════════════════════════════════════════════════════════
// 🎨 COMPONENTES UI (desde features/onboarding/components)
// ═══════════════════════════════════════════════════════════════════

export {
  IndustrySelector,
  type IndustrySelectorProps,
} from '~/features/onboarding/components/industry-selector';

export {
  OnboardingProgress,
  type OnboardingProgressProps,
} from '~/features/onboarding/components/onboarding-progress';
